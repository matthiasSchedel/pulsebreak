# Pulsebreak public listing journeys

## Public listing routes

Open the exact-head preview and verify:

1. `/` loads the Pulsebreak title artifact and every referenced local asset succeeds.
2. `/support/` names the shipped support contact and links back to the game.
3. `/privacy/` truthfully states that the shipped app does not collect data and links back to the game.
4. Reloading those routes introduces no cross-origin runtime dependency.

This generated deployment does not change game visuals or mechanics. The native and browser candidate remains bound to the source repository's independent screenshot and gameplay evidence.
